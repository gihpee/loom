# $connection_upgrade — «upgrade» только когда клиент этого просил, иначе
# «close». Захардкоженный «upgrade» ломает обычные запросы на некоторых
# клиентах, и ловится это через раз.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
